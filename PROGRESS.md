# تقدّم العمل — جيبي / Jeybi (المرحلة الأولى)

آخر تحديث: 2026-09-25 — **«اكتب يومك» (الإدخال الذكي) مضاف**، وتطبيق Android منشور في Releases (انظر قسمي ✍️ و📱). الوسم `phase1-stable` للرجوع.

## ✅ حالة أقسام jeybi-phase1.md

| القسم | الحالة | أين |
|---|---|---|
| §1 PWA، offline، Repository منفصل | ✅ مُختبَر: التطبيق يعمل بعد إيقاف السيرفر | `vite.config.ts`، `src/repo/*` |
| §2 لغتان، RTL/LTR، أرقام لاتينية، MRU، ×100 | ✅ مُختبَر (اختبارات + متصفح) | `src/lib/money.ts`، `src/i18n/*` |
| §3 المحافظ + التحويل + الرسوم | ✅ | `src/repo/wallets.ts`، `src/repo/transactions.ts`، `src/pages/Manage.tsx` |
| §4 التصنيفات الرئيسية والفرعية، أرشفة بدل حذف | ✅ | `src/repo/categories.ts`، `src/pages/Manage.tsx` |
| §5 إدخال سريع، تفاصيل، وسوم، تقسيم، قوالب | ✅ مُختبَر في المتصفح | `src/components/TxEditor.tsx`، `src/repo/templates.ts` |
| §6 تراجع، حذف ناعم، محذوفات، سجل | ✅ | `src/components/Toast.tsx`، `src/pages/Trash.tsx`، `src/pages/TxDetail.tsx` |
| §7 بحث وفلاتر مع المجموع | ✅ | `src/pages/Transactions.tsx`، `filterTransactions` |
| §8 التقارير، "أين ذهب مالي؟"، Excel/PDF/صورة | ✅ مُختبَر: PDF صفحتان بعربية سليمة، PNG، Excel | `src/pages/Reports.tsx`، `src/components/ReportPrint.tsx`، `src/services/{pdf,excel,share}.ts` |
| §9 الشاشة الرئيسية | ✅ | `src/pages/Home.tsx` |
| §10 PIN، البصمة، نسخ احتياطي، تذكير، persist | ✅ PIN مُختبَر؛ البصمة **تحتاج تجربة على Chrome Android** | `src/components/Lock.tsx`، `src/pages/Security.tsx`، `src/pages/Backup.tsx`، `src/services/security.ts` |
| §11 الإعدادات، الوضع الليلي، حجم الخط، الترحيب | ✅ مُختبَر (عربي فاتح + فرنسي داكن) | `src/pages/Settings.tsx`، `src/pages/Onboarding.tsx` |
| §12 اختبارات + بيانات تجريبية | ✅ 55 اختبارًا ناجحًا | `src/tests/*`، `src/repo/demo.ts` |
| §13 النشر + README | ✅ مرفوع إلى https://github.com/yede3333-star/jeybi ومنشور تلقائيًا عبر GitHub Actions على https://yede3333-star.github.io/jeybi/ | `.github/workflows/deploy.yml`، `netlify.toml`، `README.md` |

## 🐞 أخطاء وجدتها التجربة الفعلية وأُصلحت

- `useEffect(() => window.scrollTo(...))`: في Chrome الحديث تُرجع `scrollTo` وعدًا (Promise) فيتعطّل React ← صار داخل أقواس.
- القفل كان يُطلب فورًا بعد إنشاء الرمز في الترحيب ← `markUnlocked()`.
- عدد البيانات التجريبية لم يحتسب عمليات الرسوم المرتبطة، والبيانات كانت تُنتج أرصدة سالبة ← أُعيد ضبطها + اختبار يضمن عدم السلبية.
- صياغة "أين ذهب مالي؟" ("لـإيجار") وتكرار "أخرى" ← "إيجار 16%" و"باقي التصنيفات".
- التصدير كان ينتظر `requestAnimationFrame` ← `setTimeout`.

## 🆕 الجلسة الثالثة

- `src/components/DatePicker.tsx`: `DateField` و`DateTimeField` (تقويم خاص + ساعة 24h) بدل كل حقول `type="date"`/`datetime-local` — الأرقام لاتينية دائمًا مهما كانت لغة الهاتف. مُختبَر في المتصفح.
- مهلة القفل الافتراضية: 5 دقائق (`DEFAULT_SETTINGS.lockTimeoutMin`).
- العملة: بدون تغيير بقرار المستخدم.

## 🆕 الجلسة الرابعة — ملاحظات الهاتف

### البحث لا يجد "بقالة" — السبب
الكلمة لم تكن مخزّنة في العملية أصلًا: الإدخال السريع ملاحظته فارغة وبلا وسوم، و"بقالة" هو **اسم التصنيف الفرعي** المعروض. والتصنيف الافتراضي مخزّن بـ `name: ` و`sysKey: groceries` ويُترجم عند العرض فقط. البحث القديم كان يفحص `note` و`tags` فقط، فلم تُعثر على 120 عملية.
الإصلاح: `src/services/search.ts` — يبحث في الأسماء المعروضة (بالعربية والفرنسية) للتصنيفات وآبائها والمحافظ والقوالب (`templateId` جديد على العملية)، مع تطبيع العربية والفرنسية، وأرقام مشرقية، والمبالغ بكل الصيغ مع أجزاء التقسيم. الاختبارات: `src/tests/search.test.ts`.

### البطء عند كل فتح — ما قيس وما وُجد
- المكتبات الثقيلة (Recharts, xlsx, jsPDF, html2canvas) **كانت أصلًا** تُحمَّل عند الطلب. لكن كل الصفحات كانت في الحزمة الأولى.
- PBKDF2 بـ 210,000 تكرار ثابتة عند كل فتح (≈300ms على الحاسوب، ≈1–2 ث على هاتف متوسط).
- الرئيسية كانت تقرأ **كل العمليات 3 مرات** (الأرصدة، آخر العمليات، بانر النسخ) وتعيد الحساب عند كل تغيير.
- إعدادات التطبيق كانت تُقرأ من كل جدول meta.

الإصلاحات:
- `src/repo/flows.ts`: أرصدة المحافظ مخزّنة ومحدَّثة داخل نفس معاملة كل كتابة (`putTx/patchTx/deleteTxRow`)، وتُعاد من الصفر بعد العمليات الجماعية. اختبار تطابق مع إعادة الحساب الكامل بعد كل نوع عملية (`src/tests/flows.test.ts`).
- `src/repo/summary.ts`: اليوم/الشهر عبر فهرس `date`، آخر العمليات بـ `limit`.
- PIN: عدد التكرارات يُعايَر على الجهاز نفسه (~150ms، بين 20k و600k)، والرموز القديمة تُعاد تجزئتها مرة واحدة بعد أول فتح ناجح (`pinCalibrated`).
- كل الصفحات عدا الرئيسية + نافذة الإدخال في حزم منفصلة (الإدخال يُحمَّل مسبقًا عند الخمول).
- `getSettings` يقرأ مفاتيح الإعدادات فقط؛ `initDatabase` قراءتان سريعتان عند كل فتح، والعمل الثقيل مرة واحدة.
- هيكل ثابت في `index.html` يظهر قبل تحميل JavaScript.

### القياسات (هذا الحاسوب، Chrome، تقييد المعالج 4x، 2,345 عملية ≈ سنتان ونصف)
| المقياس | قبل | بعد |
|---|---|---|
| فتح التطبيق المثبّت: حتى ظهور شاشة القفل | 210ms | 227ms |
| من إدخال الرمز حتى رئيسية قابلة للاستعمال | 907ms | 223ms |
| المجموع | 1128ms | 446ms |
| JS المحمّل عند الفتح (غير مضغوط) | 614KB | ≈496KB |
| Lighthouse (هاتف، 4G بطيء، أول تحميل): النتيجة / FCP / LCP / TBT | 96 / 2.2s / 2.4s / 60ms | 94–96 / 2.0–2.1s / 2.5–2.6s / 30–130ms |

ملاحظة: Lighthouse يقيس **أول تحميل عبر الشبكة**؛ لم يتغيّر كثيرًا لأن المكتبات الثقيلة كانت مؤجّلة أصلًا (والفروق ضمن التذبذب بين التشغيلات). المكسب الحقيقي في الفتح المتكرر. تقييد CDP لا يبطئ WebCrypto، لذا زمن PBKDF2 الفعلي على الهاتف أعلى — لهذا صارت المعايرة على الجهاز نفسه.

## 🚧 المرحلة الثانية (jeybi-phase2.md) — التقدّم

الوسم `phase1-stable` (= ae5b82e) مرفوع للرجوع إليه.

| القسم | الحالة |
|---|---|
| 0. الترحيل والنسخ الاحتياطي | ✅ `db.version(2)` (جداول: debts, budgets, recurring, pending, goals, goalMoves + فهارس debtId/recurringKey) — الترقية تضيف فقط 3 تصنيفات نظام (zakat, adjustment_expense, adjustment_income). النسخ الاحتياطي format 2 ويقبل format 1. **اختبار `src/tests/migration.test.ts`** على قاعدة مرحلة أولى حقيقية (`src/tests/fixtures/phase1-db.json` مولّدة بكود phase1-stable): الصفوف متطابقة، الأرصدة والمجاميع والتقرير متطابقة، المحذوفات والسجل وPIN محفوظة، والترقية لا تتكرر، واستيراد نسخة مرحلة أولى ينجح. |
| 1. الديون | ✅ المنطق ✅ (`src/repo/debts.ts`, `debtSync.ts`؛ سداد جزئي، إغلاق تلقائي، إعادة فتح عند حذف دفعة، دين بلا محفظة) — الواجهة ✅ (مُجرَّبة في المتصفح) |
| 2. الميزانيات | ✅ المنطق ✅ (`src/repo/budgets.ts`؛ الأب يشمل الفرعية، تنبيه 80/100 مرة واحدة، نسخ الشهر الماضي) — الواجهة ✅ (مُجرَّبة في المتصفح) |
| 3. المتكررة | ✅ المنطق ✅ (`src/repo/recurring.ts`؛ عدّاد + مفتاح ثابت = لا تكرار، تعويض الأيام الفائتة، وضع التأكيد، 31 → 28) — الواجهة ✅ (مُجرَّبة في المتصفح) |
| 4. الادخار | ✅ المنطق ✅ (`src/repo/goals.ts`؛ المبلغ الشهري المطلوب، "مخصص" لكل محفظة، حدود الإيداع/السحب) — الواجهة ✅ (مُجرَّبة في المتصفح) |
| 5. المطابقة | ✅ المنطق ✅ (`src/repo/reconcile.ts`؛ الفرق، تسوية كمصروف/دخل غير مسجَّل، تذكير 14 يومًا) — الواجهة ✅ (مُجرَّبة في المتصفح) |
| 6. التذكير اليومي | ✅ بانر في الرئيسية (`src/repo/homeExtras.ts`) + إشعار تجريبي من الإعدادات (الإشعار يحتاج تجربة على الهاتف) |
| 7. المؤشرات | ✅ المنطق ✅ (`src/services/insights.ts`؛ يقرأ آخر 35 يومًا فقط، عتبة، 3 ملاحظات كحد أقصى) — الواجهة ✅ (مُجرَّبة في المتصفح) |
| 8. العملات | ✅ المنطق ✅ (`src/services/currency.ts`؛ سعر ×10000 صحيح، BigInt، origCurrency/origAmount/rateE4) — الواجهة ✅ (مُجرَّبة في المتصفح) |
| 9. الزكاة | ✅ المنطق ✅ (`src/services/zakat.ts`, `src/repo/zakat.ts`؛ النصاب، 2.5% بأعداد صحيحة، الحول بأم القرى) — الواجهة ✅ (مُجرَّبة في المتصفح) |
| 10. التكامل | ✅ تقارير (`ReportExtras`) + Excel (أوراق الديون/الميزانيات/الأهداف) + البحث (أسماء الأشخاص والأهداف والمبلغ الأصلي) + البيانات التجريبية + الترجمة كاملة + README + قياس الأداء + لقطات شاشة باللغتين (عربي فاتح، فرنسي داكن) |

**اختبار التحديث في متصفح حقيقي (Chrome، Puppeteer):** نسخة المرحلة الأولى مع بيانات وPIN ← استبدال الخادم بنسخة المرحلة الثانية على نفس العنوان ← فتح عادي ثم إغلاق وإعادة فتح. النتيجة: المخطط 1 → 2، العمليات الـ469 متطابقة حرفيًا، المحذوفات محفوظة، الأرصدة والإجمالي (107,310) ومجاميع الشهر متطابقة، الرمز يُطلب **مرة واحدة**، النسخة القديمة لم تُعِد التحميل أثناء الاستعمال، والصفحات الجديدة تعمل. (السكربت: `scratchpad/perf/upgrade-e2e.mjs`؛ يحتاج puppeteer-core.)

**خطأ وجده هذا الاختبار وأُصلح:** `registerType: autoUpdate` كان يعيد تحميل الصفحة بعد ثوانٍ من الفتح (قد يضيع إدخال نصف مكتوب، ويُطلب الرمز مرتين). صار `prompt`: النسخة الجديدة تنتظر وتُطبَّق عند الإغلاق وإعادة الفتح، أو فورًا من بانر "نسخة جديدة جاهزة". وفتح القفل يُحفظ في sessionStorage لدقيقة حتى لا تعيد إعادة التحميل القفل.

**زمن الفتح (نفس طريقة المرحلة الأولى، نفس الحاسوب، CPU ×4، 2,345 عملية، وسيط 3 مرات):**

| المقياس | المرحلة الأولى (phase1-stable) | المرحلة الثانية |
|---|---|---|
| الفتح حتى شاشة القفل | 208ms | 249ms |
| من الرمز حتى رئيسية قابلة للاستعمال | 327ms | 270ms |
| المجموع | 535ms | 552ms |
| Lighthouse (هاتف، أول تحميل) النتيجة / FCP / LCP | 95 / 2.1s / 2.5s | 94 / 2.1s / 2.6s |
| JS المنقول (مضغوط) | 172KB | 183KB |

الفرق ضمن التذبذب بين التشغيلات (500–581ms). التذكيرات والمؤشرات والأدوات تُحمَّل بعد ظهور الرئيسية، والمتكررة تُولَّد في الخلفية. (السكربت: `scratchpad/perf/measure.mjs`، يمسح sessionStorage قبل كل فتح لمحاكاة فتح بارد.)

**لقطات الشاشة (Puppeteer):** كل الصفحات الجديدة بالعربية (فاتح) والفرنسية (داكن). أُصلح منها: نسبة مكررة في تنبيه الميزانية، وصياغة "سلف لـ فلان". (السكربت: `scratchpad/perf/shots.mjs`.)

قرارات المرحلة الثانية:
- نوع عملية جديد `debt` (مع `debtId` و`flow: in|out`): يحرّك المحفظة ولا يُحسب دخلًا ولا مصروفًا أبدًا (`walletDeltas`/`totalsFor`/`timeline`/`byWallet`). لا يُعدَّل من نافذة الإدخال (EditType يستثنيه).
- `missingSystemCategories()` في `src/data/defaults.ts` هي المصدر الوحيد لتصنيفات النظام الجديدة (الترقية + الاستيراد).
- لا تعدّل `SCHEMA_V1` ولا `version(2)` أبدًا؛ أي تغيير لاحق = `version(3)`.
- `wipeAll` يمسح كل الجداول (`db.tables`).
- **التحديثات:** وضع `prompt` في `vite.config.ts` + `UpdateBanner` في `App.tsx`. لا ترجع إلى `autoUpdate`.
- المؤشرات تُعرض بوحدات كاملة (بلا كسور)، وملاحظات التصنيف فقط للمصاريف المنتظمة (3 من 4 أسابيع).
- اختبارات المنطق: `src/tests/phase2.test.ts` (26 اختبارًا). كل استدعاء لـ `createTransaction` داخل معاملة خارجية يجب أن يشمل نطاقها `db.debts` (لأن الحذف/الاسترجاع يزامن الديون).
- المتكررة: `generated` عدّاد + `occurrenceKey` = `ruleId:yyyy-MM-dd` على العملية (`recurringKey` مفهرس). لا تعبئة لما قبل اليوم عند الإنشاء.
- العملة الأساسية = `settings.currency`؛ العملات الإضافية في `settings.currencies` وآخر سعر في `settings.lastRates`.

## 🆕 الجولة الثالثة — ملاحظات الهاتف بعد المرحلة الثانية

### أسباب المشاكل الأربع وإصلاحها

1. **تصدير النسخة الاحتياطية لا يفعل شيئًا** — ثلاثة أسباب معًا: (أ) `navigator.share()` كان يُستدعى بعد التجهيز الطويل (قراءة الجداول + تحويل الصور base64)، فتضيع "لمسة المستخدم" ويرفض المتصفح المشاركة؛ (ب) Chrome على Android لا يقبل مشاركة ملفات `.json` ولا `.xlsx` أصلًا (قائمة أنواع مسموحة)؛ (ج) الرفض كان يُعامل كـ"إلغاء" بلا أي رسالة، والتنزيل البديل (رابط مخفي) لا يعمل دائمًا في التطبيق المثبّت. **الإصلاح:** `src/components/FileShare.tsx` — خطوتان: تجهيز الملف ← نافذة فيها "مشاركة الآن" (تُستدعى `share()` مباشرة داخل اللمسة) و"تنزيل الملف"، ورسالة واضحة في كل حالة (تمت / أُلغيت / خطأ + جرّب التنزيل / بدأ التنزيل). النسخة تُشارَك باسم `.txt` (نوع مسموح) والاستيراد يقبل `.json` و`.txt`. نفس المسار لصورة التقرير وPDF وExcel.
2. **المثبّت أبطأ من المتصفح** — القياس لم يجد فرقًا جوهريًا في الشيفرة (نفس الملفات، كلها من الكاش، `start_url`/`scope`/نطاق SW = `/jeybi/`). السبب العملي: المثبّت يُفتح غالبًا "باردًا" (العملية مغلقة) بينما تبويب المتصفح يبقى دافئًا، + فحص البصمة البطيء على Android (انظر 4). طلب التخزين الدائم صار مؤجَّلًا لوقت الفراغ. **جرّبت** تحميل قاموس اللغة الحالية فقط، فكان الفتح البارد **أبطأ** (طلب متسلسل إضافي عبر SW قبل أول رسم) — فأُلغي وبقي القاموسان في الحزمة. ملاحظة: Chrome 153 يرسل طلب "preload" للصفحة بالتوازي، لكن الصفحة تُخدم من الكاش (`deliveryType: cache-storage`) والتطبيق يعمل دون إنترنت.
3. **الوصل بالكاميرا فقط** — كان `capture="environment"` مفروضًا على الحقل الوحيد. صار زرّان: "تصوير" (مع capture) و"من المعرض" (بدونه)، وكلاهما يمر بـ `compressImage`.
4. **البصمة تبطئ الفتح** — شاشة القفل كانت تنتظر `isUserVerifyingPlatformAuthenticatorAvailable()` (بطيء على Android) قبل إظهار زر البصمة وطلبها. صار: وجود `bioCredentialId` يكفي، الشاشة ترسم فورًا، الطلب يبدأ بعد أول رسم وفقط والتطبيق ظاهر، والفتح بالبصمة لا يشغّل PBKDF2. الإلغاء/الفشل يعرض فورًا "أدخل الرمز".

### أخطاء إضافية وُجدت وأُصلحت
- **إدخال الرمز من لوحة المفاتيح قد يضيع** (وُجد بقياس الأداء، موجود في النسخة السابقة أيضًا): مستمع المفاتيح كان يُركَّب بعد الرسم، و`press` يقرأ طول الرمز القديم. صار `useLayoutEffect` + تحديث وظيفي.
- حذف محفظة/تصنيف مرتبط بدين أو متكررة أو ميزانية أو هدف كان يترك مراجع معلّقة ← الآن يُؤرشف بدل الحذف.
- تغيير اتجاه دين (لي ↔ عليّ) لم يقلب حركات السداد ← يُقلب الآن، واختبار سلامة الأرصدة يغطيه.
- مبالغ ضخمة: حد أقصى `MAX_AMOUNT` (10¹² أوقية) في كل المدخلات (رسالة `errors.tooLarge`).
- الرئيسية لا تتحدّث بعد منتصف الليل أو بعد ترك الهاتف ليلة ← `useDayKey()`/`useHourKey()`.
- أخطاء صامتة (`catch {}`) ونصوص أخطاء خام ← `errorMessage()` برسائل مترجمة، `ErrorBoundary`، ومعالج عام يسجّل كل خطأ غير متوقَّع.
- نافذة المشاركة كانت تعود للظهور إن أُغلقت أثناء التجهيز.
- اتجاه اسم الملف وحجمه في العربية ("KB 244"، واسم تقرير PDF مقلوب) ← `<bdi dir="ltr">`.
- "Depuis la galerie" تلتف على سطرين ← "Galerie".
- CI يشغّل الاختبارات أيضًا بتوقيت نواكشوط وبتوقيت UTC+14 (نجحت محليًا أيضًا بـ UTC−7). اختبار الترحيل يقارن بأرقام المرحلة الأولى المحسوبة بتوقيت المستخدم، فيثبّت `process.env.TZ` على نواكشوط.

### سجل الأخطاء
`src/services/errorLog.ts` — localStorage (`jeybi:errors`)، آخر 50، الوقت + الشاشة (بلا query) + الرسالة مع حجب أي رقم من 3 خانات فأكثر. صفحة `#/settings/errors` بنسخ/مشاركة (نص)/مسح.

### الاختبارات الجديدة
- `src/tests/integrity.test.ts`: 250 عملية عشوائية × 4 بذور (دخل، مصروف، تحويل برسوم، عملة أجنبية، دين، سداد جزئي، قلب اتجاه دين، مطابقة، حذف واسترجاع، أهداف) — الرصيد = دفتر مستقل = إعادة الحساب من الصفوف. + حالات حدّية (صفر، حد أقصى، أرشفة المراجع).
- `src/tests/backupCycle.test.ts`: تصدير ← مسح ← استيراد يعيد كل الجداول والصور والعملات والأسعار والزكاة حرفيًا، ويحافظ على رمز الجهاز، والاستيراد مرتين لا يكرّر.
- `src/tests/errorLog.test.ts`: لا مبالغ ولا query في السجل، 50 كحد، لا يرمي أبدًا.
- لقطات (`scratchpad/perf/shots3.mjs`): الإعدادات، سجل الأخطاء، النسخ الاحتياطي، نافذة المشاركة، PDF، نافذة الإدخال، شاشة القفل — عربي/فرنسي × فاتح/داكن، مع فحص تجاوز العرض 390px (لا شيء) وأخطاء الـ console (لا شيء).

### زمن الفتح البارد (Chrome 153، CPU ×4، 2,345 عملية، وسيط 7 فتحات، `scratchpad/perf/standalone.mjs`)

"شاشة القفل" = من فتح الرابط حتى ظهور شاشة القفل؛ "جاهزة" = حتى ظهور الرئيسية بعد الرمز (4 لمسات) أو البصمة. "مثبّت" = نافذة تطبيق `--app` (أقرب ما يمكن للمثبّت على الحاسوب). القياسان متتاليان على نفس الحاسوب.

| الحالة | قبل (المرحلة 2): القفل / جاهزة | بعد: القفل / جاهزة |
|---|---|---|
| متصفح + رمز | 802 / 1309ms | 711 / 1251ms |
| مثبّت + رمز | 822 / 1369ms | 878 / 1439ms |
| متصفح + بصمة | 869 / 1269ms | 846 / 1279ms |
| مثبّت + بصمة | 847 / 1228ms | 909 / 1347ms |

**القراءة الأمينة:** الفروق كلها (±100ms) ضمن التذبذب بين الفتحات (مدى 7 فتحات يصل إلى 300ms)، فلا ربح ولا خسارة يمكن إثباتهما على الحاسوب، ولا فرق ثابت بين المتصفح ونافذة التطبيق. البصمة الافتراضية في Chrome تجيب فورًا ولا تحاكي بطء Android، فأثر إصلاح البصمة لا يُقاس هنا. **لذلك أُضيف قياس على الهاتف نفسه:** `src/services/startupTiming.ts` يسجّل آخر 10 فتحات (مثبّت/متصفح، زمن أول شاشة، وزمن الرئيسية بعد الرمز أو البصمة) ويعرضها في صفحة سجل الأخطاء، وتُنسخ معه.

### ⚠️ النشر لم يتم فعليًا حتى 2026-09-25
الدفعتان `5eec6a9` و`a50c827` فشلتا في CI فلم تُنشرا (الهاتف كان يعمل بنسخة المرحلة الثانية `e3c8893`): الأولى فشلت في اختبار الترحيل بتوقيت UTC+14، والثانية أصلحته لكن `tsc` في `npm run build` رفض `process` (لا أنواع Node في tsconfig التطبيق). أُصلح في `27eab2f` بالوصول إلى `process.env` عبر `globalThis`، وCI نجح ونُشر. **درس:** بعد كل push تحقّق من نتيجة CI (`https://api.github.com/repos/yede3333-star/jeybi/actions/runs?per_page=3`)، ولا تكتب "منشور" قبل نجاحها، وشغّل `npm run build` (فيه `tsc`) لا الاختبارات وحدها.

**اختبار التحديث** (`scratchpad/perf/upgrade3.mjs`): نسخة المرحلة 2 مع بيانات تجريبية ← استبدال الخادم بهذه النسخة ← فتح (النسخة الجديدة تنتظر) ← إغلاق وفتح: النسخة الجديدة تعمل، الجداول الـ11 متطابقة حرفيًا (474 عملية، الديون…)، وصفحة سجل الأخطاء تعمل.

## 📱 تطبيق Android (jeybi-android.md)

| القسم | الحالة | أين |
|---|---|---|
| 1. الإعداد | ✅ Capacitor 8، `com.yede.jeybi`، الاسم "جيبي" (fr/en: Jeybi)، أيقونات وشاشة بداية من أيقونة التطبيق (فاتح/داكن)، عمودي، شريط الحالة يتبع ثيم التطبيق، minSdk 24 (Android 7+)، زر الرجوع: يغلق النافذة المفتوحة ← يرجع ← في الرئيسية ضغطتان للخروج، وفي شاشة القفل يخرج | `capacitor.config.ts`، `android/`، `src/platform/native.ts` (`onBack`)، `scripts/make-icons.mjs` |
| 2. طبقة المنصة | ✅ `src/platform/index.ts` (`isNative`، `native()` يحمّل الإضافات داخل التطبيق فقط) — البصمة BiometricPrompt، الإشعارات، المشاركة/التنزيلات، الاستيراد (منتقي الملفات دون فلتر)، الكاميرا والمعرض | `src/platform/*`، `DownloadsPlugin.java` |
| 3. البيانات | ✅ IndexedDB داخل بيانات التطبيق الخاصة (لا تُمسح إلا بحذف التطبيق)، `allowBackup=false`، "مسح كل البيانات" بكتابة كلمة، نسخ مشفّر AES-GCM + PBKDF2 600k، نسخ تلقائي أسبوعي إلى Documents/Jeybi (آخر 4) + إشعار للمشاركة | `src/services/backupCrypto.ts`، `src/repo/phone.ts`، `src/pages/Backup.tsx`، `src/pages/Settings.tsx` |
| 4. البناء على GitHub | ✅ الـ workflow: test ← (web → deploy) و (android → Release). APK موقّع عند وجود السرّين | `.github/workflows/deploy.yml` |
| 5. التحقق | انظر أدناه | |

### ما تم التحقق منه وما لم يُتحقق منه
- ✅ 148 اختبارًا (منها 11 جديدة في `src/tests/android.test.ts`: التشفير ذهابًا وإيابًا، كلمة مرور خاطئة لا تغيّر شيئًا، ملف معدَّل يُرفض، المفتاح المخزّن، النسخ القديمة غير المشفّرة، خطة الإشعارات).
- ✅ نسخة الويب في المتصفح: إنشاء نسخة مشفّرة، رسالة كلمة المرور الخاطئة، المعاينة بعد كلمة المرور الصحيحة، المسح بكتابة «مسح» يعيد الترحيب. لا أخطاء في الـ console. حجم JS عند الفتح زاد ~6KB فقط (@capacitor/core).
- ✅ GitHub Actions (`2224f70`): test و web و deploy و **android ناجحة** — Gradle يبني الـ APK (غير موقّع حتى الآن لأن السرّين غير مضافين). أول بناء فشل بسبب `--` داخل تعليق XML وأُصلح. نسخة الويب المنشورة تعمل (لا أخطاء console).
- ✅ **التوقيع والنشر** (`84f77f1`، 2026-09-25): السرّان مضافان، الـ 4 jobs ناجحة، `apksigner verify` نجح في CI، والإصدار **v1.0.29** منشور: https://github.com/yede3333-star/jeybi/releases/tag/v1.0.29 (`jeybi.apk`، 7.5MB). تحقّقت محليًا أن شهادة المفتاح (SHA-256 `77:CF:F9:26:…:59:7A`) موجودة في كتلة توقيع v2/v3 داخل الملف المنشور.
- ⏳ **لا يوجد Android SDK ولا JDK على هذا الحاسوب** (بطلب المستخدم): بناء الـ APK يُتحقق منه على GitHub Actions فقط، والسلوك على الهاتف (البصمة، الإشعارات، المشاركة، الكاميرا، Documents/Jeybi) يحتاج تجربة المستخدم.

### مفتاح التوقيع
- `C:\Users\HP\Documents\Jeybi-signing-key\` (خارج المستودع): `jeybi-release.p12` (PKCS12، alias `jeybi`، RSA 4096، 10000 يوم، مولَّد بـ openssl لعدم وجود keytool)، `password.txt`، `jeybi-release.p12.base64.txt`، و"اقرأني - مهم جدا.txt".
- السرّان في GitHub: `ANDROID_KEYSTORE_BASE64` و`ANDROID_KEYSTORE_PASSWORD` — يضيفهما المستخدم بنفسه (الخطوات في README وفي ملف اقرأني). دونهما: APK غير موقّع يُحفظ كـ artifact 7 أيام، ولا Release.

### قرارات Android
- **لا تغيّر** `appId` ولا `server.hostname`/`androidScheme` في `capacitor.config.ts`: أصل قاعدة البيانات `https://localhost`؛ تغييره = تطبيق فارغ بعد التحديث.
- لا Service Worker داخل التطبيق (`main.tsx`): التحديث = APK جديد. `UpdateBanner` للويب فقط.
- `versionCode` = `git rev-list --count HEAD` (يحتاج `fetch-depth: 0`)، `versionName` = `1.0.<code>`، والوسم `v1.0.<code>`. نفس الرقم يظهر أسفل الإعدادات (`__APP_BUILD__`). لا تعِد كتابة تاريخ `main` (ينقص العدد فيرفض Android التحديث).
- ملف الـ APK في كل Release اسمه ثابت `jeybi.apk` ليعمل الرابط `releases/latest/download/jeybi.apk`.
- الإشعارات: 30 تذكيرًا يوميًا منفصلًا (لتخطّي اليوم إن سُجّلت عملية)، يُعاد التخطيط عند كل فتح/تغيير (`PhoneJobs` في `App.tsx`، مؤجَّل 1.5 ث). الديون عند 09:00 يوم الاستحقاق، الحول عند 09:00 يوم اكتماله. تنبيه الميزانية يبقى داخل التطبيق (يحدث فقط عند إضافة عملية والتطبيق مفتوح). `USE_EXACT_ALARM` مسموح لأن التطبيق خارج متجر Play — إن نُشر على Play يجب مراجعته.
- مفتاح النسخ التلقائي: `CryptoKey` غير قابل للتصدير في `meta` (`autoBackupKey`، ضمن `DEVICE_ONLY_KEYS`)؛ كلمة المرور لا تُخزَّن أبدًا. النسخ التلقائي يُفحص عند الفتح (لا تشغيل في الخلفية).
- لا صلاحية CAMERA في الـ manifest عمدًا (إضافة الكاميرا تستعمل تطبيق الكاميرا دون صلاحية).
- القفل بعد الخمول صار **طبقة فوق التطبيق** (بعد أول فتح) بدل استبداله: إدخال نصف مكتوب أو صورة وصل لا تضيع.
- إضافة Sheet جديدة تحصل على زر الرجوع تلقائيًا (`pushBackHandler` في `Sheet`).
- لا Java/Android SDK محليًا: أخطاء Gradle تظهر كـ annotation عامة في GitHub (`Gradle build failed`)، تُقرأ من `/check-runs/<job id>/annotations` دون تسجيل دخول.

## ✍️ اكتب يومك (jeybi-smart-input.md)

| القسم | الحالة | أين |
|---|---|---|
| 1. الشاشة | ✅ زر «اكتب يومك» بجانب + في الرئيسية، خانة نص بمثال، بطاقات فورية أثناء الكتابة (useDeferredValue) + زر تحليل، النص مع تلوين ما فُهم (أخضر) وما لم يُفهم (كهرماني) + «إضافة كعملية»، تعديل البطاقة بنفس محرر العمليات (وضع `draft`/`onDraft` في `TxSheet`)، الديون بنافذة خاصة، حذف، «حفظ الكل» ذرّي مع تراجع واحد، الجملة كاملة في `sourceText` على كل عملية (والبحث يقرؤها)، والمسودة محفوظة في `localStorage` (`jeybi:smart-draft`) | `src/pages/SmartEntry.tsx`، `src/repo/smart.ts` |
| 2. المحلل | ✅ تقسيم بالفواصل (دون كسر 5,000) ثم بالمبالغ و«و»؛ أرقام لاتينية/مشرقية/بالكلمات (فصحى، حسانية، فرنسية)؛ عملات أجنبية بآخر سعر؛ أوقية قديمة ÷10؛ دخل/مصروف/تحويل/دين؛ تصنيف بأولوية: كلمة متعلَّمة > أسماء المستخدم > القاموس > الأسماء المعروضة، والفرعي قبل الأب؛ محفظة بدورها (من/إلى)؛ تاريخ يسري على ما بعده؛ تعلّم محلي | `src/services/smartParse.ts`، `src/services/smartLexicon.ts`، `src/pages/SmartWords.tsx` |
| 3. الصوت | ✅ Android: `@capgo/capacitor-speech-recognition` (نافذة Google، `ar-SA`/`fr-FR`)؛ الويب: Web Speech API إن وُجد وإلا يختفي الزر؛ رسائل واضحة (غير متاح، مرفوض، يحتاج إنترنت، لم أسمع) | `src/services/speech.ts`، `src/platform/native.ts` |
| 4. المشاركة إلى جيبي | ✅ intent-filter لـ `text/plain` + `ShareInPlugin.java` (أول فتح و`onNewIntent`) ← `#/write` والنص جاهز، والقفل أولًا | `android/…/ShareInPlugin.java`، `AndroidManifest.xml` |
| 5. الجودة | ✅ `src/tests/smartParse.test.ts`: 57 جملة + 11 اختبار تفاصيل (المثال الإلزامي يعطي 4 عمليات صحيحة، 60 عملية في أقل من 100ms)، `src/tests/smartSave.test.ts` (ذرّية، تراجع، بحث، النسخ الاحتياطي). 219 اختبارًا ناجحًا. حزمة الفتح +1.2KB فقط (المحلل والقاموس 33KB في حزمة الصفحة) | |

**مُجرَّب في المتصفح:** المثال + جزء غير مفهوم + يورو بلا سعر (بطاقة حمراء تمنع الحفظ) ← تعديل بالمحرر (سعر 43 وتصنيف عائلة) ← «سأتذكّر: عشاء ← عائلة» ← حفظ 5 عمليات والرصيد صحيح (790) ← التراجع يحذفها كلها ← «عشاء 300» صار عائلة تلقائيًا ← صفحة الكلمات المتعلَّمة. لا أخطاء console.
**لم يُجرَّب (يحتاج الهاتف):** الميكروفون في Android، والمشاركة من واتساب.

قرارات «اكتب يومك»:
- المحلل **خالص** (بلا قاعدة بيانات): يأخذ `SmartContext` (التصنيفات، المحافظ، الأسماء باللغتين، المحفظة الافتراضية، الأسعار، القواعد، الوقت). أضف الكلمات الجديدة في `smartLexicon.ts` (تُطبَّع تلقائيًا) وأضف جملة اختبار.
- القواعد المتعلَّمة في `settings.smartRules` (مفتاح = الكلمة مطبَّعة) — ليست ضمن `DEVICE_ONLY_KEYS` عمدًا لتدخل في النسخة الاحتياطية.
- مفتاح البطاقة `start:نص` يحفظ تعديلات المستخدم عند إعادة التحليل؛ حذف/تعديل بطاقة لا يضيع إن أُضيف نص بعدها.
- لا تُستعمل `count` في مفاتيح الترجمة (i18next يبحث عن صيغ الجمع): استعمل `{{n}}`.
- الأرقام الملتبسة (ست، سبع، un، une) لا تُقرأ أرقامًا إلا مع «مية/ألف…».

## 📌 قرارات تقنية يجب الالتزام بها

1. **Node.js v24** في `C:\Program Files\nodejs` — في Git Bash: `export PATH="/c/Program Files/nodejs:$PATH"`. ملف `.claude/launch.json` يستعمل `C:/PROGRA~1/nodejs/node.exe` لأن تطبيق Claude لا يرى Node في PATH.
2. **Tailwind v4** بدون `tailwind.config`؛ الداكن بالفئة `.dark`؛ ألوان دلالية (`bg-surface`، `bg-page`، `text-ink`، `text-muted`، `ring-line`، `.text-income/.text-expense/.text-transfer`) وفئات جاهزة (`.card .btn-primary .btn-soft .btn-ghost .btn-danger .input .label .chip .chip-on .section-title .num`). خصائص منطقية فقط (`ms-/me-/ps-/pe-/start-/end-`)، و`rtl:rotate-180` للأسهم.
3. **react-router v8 + `HashRouter`**؛ `base` = `process.env.BASE_PATH || './'`.
4. **المبالغ أعداد صحيحة ×100**؛ التنسيق عبر `useFmt()` فقط (يضيف LRI/PDI في العربية؛ `stripBidi` للنصوص الخام).
5. **كل دخل/مصروف له `splits[]`**، و`categoryIds` مشتق للفهرسة؛ التحويل `splits=[]` + `toWalletId`.
6. **رسوم التحويل** = مصروف منفصل في `sysKey='fees'` مرتبط بـ `transferId`/`feeTxId`.
7. **الواجهة لا تستورد `db`** — فقط `src/repo/*` و`src/hooks/data.ts`.
8. العناصر الافتراضية بـ `sysKey` واسمها من `sys.<sysKey>`؛ إعادة التسمية تمسح `sysKey`.
9. دوال التعديل في repo تُرجع `undo()` تُمرَّر إلى `useToast()`.
10. الإعدادات في جدول `meta`؛ `DEVICE_ONLY_KEYS` (PIN، البصمة) لا تدخل في النسخة الاحتياطية.
11. **PDF/صورة:** DOM للطباعة (`ReportPrint`، ألوان فاتحة ثابتة) ← `html-to-image` ← `jsPDF`. لا نص مباشر في PDF.
12. SheetJS من `cdn.sheetjs.com`.
13. **أداة Bash/Edit هنا تحوّل `\uXXXX` إلى أحرف فعلية** — استعمل `String.fromCharCode(...)`.
14. **الترجمة:** أضف كل مفتاح إلى `ar.ts` ثم `fr.ts` (نوعه `Dict`، فالناقص يُفشل `tsc`). مفاتيح ديناميكية: `errors.<code>`، `backup.errors.<code>`، `history.<action>`، `history.fields.<field>`، `tx.saved_<type>`، `sys.<sysKey>`.
15. **المرحلة الثانية:** أضف `this.version(2).stores({...}).upgrade(...)` في `src/data/db.ts` ولا تعدّل `version(1)`.
16. الفرع الرئيسي اسمه `main`؛ كل push إليه يختبر ويبني وينشر. المستودع: `origin` = https://github.com/yede3333-star/jeybi.git
17. **لا تستعمل `<input type="date">` أبدًا** — استعمل `DateField`/`DateTimeField`.
18. **كل كتابة على `db.transactions`** تمر عبر `putTx/patchTx/deleteTxRow` من `src/repo/flows.ts` (أو `rebuildFlows()` بعد العمليات الجماعية)، داخل معاملة تشمل `db.meta`.
19. **الرئيسية لا تقرأ كل العمليات**: استعمل `src/repo/summary.ts`. القراءة الكاملة (`useTransactions`) فقط في صفحات العمليات والتقارير.
20. **البحث** عبر `src/services/search.ts` مع `buildSearchNames` (الأسماء المعروضة باللغتين)؛ لا تبحث في `name` المخزّن وحده.
21. أي صفحة جديدة تُضاف كـ `lazy()` في `App.tsx`.
22. **مشاركة الملفات فقط عبر `useFileShare()`** (`src/components/FileShare.tsx`): دالة تجهيز تُرجع `PreparedFile`، ولا `navigator.share` بعد `await` أبدًا. الأنواع غير المسموحة على Android (json/xlsx) تُشارَك عبر `shareAs`.
23. **لا `catch {}` صامت**: الرسائل للمستخدم عبر `errorMessage(e, t, where)` (تسجّل غير المتوقَّع في سجل الأخطاء)، والأخطاء غير المعروضة عبر `logError`.
24. كل مبلغ يُتحقَّق منه مقابل `MAX_AMOUNT` من `src/repo/transactions.ts`.
25. القاموسان في الحزمة الرئيسية عمدًا (التحميل الكسول قيس أبطأ). غيّر اللغة بـ `setLanguage()`.
26. ما يعتمد على "اليوم" في الواجهة يأخذ `useDayKey()` كاعتمادية.
27. **`TZ=... npx vitest` في Git Bash لا يصل إلى Node** (يبقى UTC+0). لاختبار منطقة زمنية محليًا استعمل PowerShell: `$env:TZ='Pacific/Kiritimati'; npx vitest run`.

## ▶️ الخطوة التالية

1. المستخدم يثبّت آخر APK من Releases ويجرّب «اكتب يومك» (الكتابة، الصوت، المشاركة من واتساب، التعلّم) وقائمة Android السابقة.
2. قبل 1 أكتوبر: «مسح كل البيانات» لبدء البيانات الحقيقية.
3. توسيع القاموس من الجمل الحقيقية التي يخطئ فيها المحلل (أضف كل جملة كاختبار).
